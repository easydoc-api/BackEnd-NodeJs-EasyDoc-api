// Responsável por retornar um prontuario expecífico por idade gestacional

import { Request, Response } from "express";

export const chartListByAgeGestationalController = async (
  req: Request,
  res: Response
) => {
  const hi = "HI";
};
